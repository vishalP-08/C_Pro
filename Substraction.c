// Substracting two numbers 
#include<stdio.h>
void main(){
    int num1 , num2 , sub;
    printf("Enter two numbers : ");
    scanf("%d%d",&num1,&num2);
    // substracting two numbers and storing the resut in sub:

    sub = num1 - num2;

    // printing the result stored in sub :

    printf("The substraction of two numbers is : %d ", sub);
}